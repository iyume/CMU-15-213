{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "lab0 make",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/0cprogramminglab"
            }
        },
        {
            "label": "lab1 make",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/1datalab"
            }
        }
    ]
}